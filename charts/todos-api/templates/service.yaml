apiVersion: v1
kind: Service
metadata:
  name: {{ include "todos-api.fullname" . }}
  namespace: distribuidos
spec:
  selector:
    app: {{ include "todos-api.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
